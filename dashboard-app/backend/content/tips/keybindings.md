# キーバインド Tips

Claude Code のよく使うキーバインドのメモです。

## 基本

- **`!<command>`** — シェルコマンドをそのセッション内で実行
- **Shift+Tab** — auto-accept / plan モードの切り替え
- **Ctrl+C** — 実行中のタスクを中断

## 会話管理

- **`/clear`** — 会話履歴をクリア（コンテキストをリセット）
- **`/compact`** — 履歴を圧縮して context を節約
